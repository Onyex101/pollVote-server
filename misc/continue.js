const { Poll } = require('../models/poll');
const { Timer } = require('./countdown');

// var checkPoll = () => {
//     Poll.find().then((polls) => {
//         polls.forEach((elem) => {

//         });
//     })
// }
